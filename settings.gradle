pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = "https://maven.fabricmc.net/" }
    }

}

rootProject.name = "${mod_name}"

include "lattice-camera"
include "lattice-chunk"
include "lattice-core"
include "lattice-entity"
