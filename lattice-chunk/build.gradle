archivesBaseName = lattice_archivesBaseName
description = lattice_description

loom {
    accessWidenerPath = file "src/main/resources/lattice-chunk.accesswidener"
}

dependencies {
    implementation project(":lattice-core")
}
